#
# @desc
# Shows the prompt styles that are usable by the user.
#
# @param mixed $1
# If omitted will display all prompt options.
# If "list", it will only display the list containing the index and its name.
# If "index" (0, 1, 2...) will only present the respective indicated index prompt.