#
# @desc
# Save the currently configured prompt settings to the
# configuration of the "myShellEnv" instance.
#
# Just in case, it will create a backup file inside the base directory
# of your "myShellEnv" whose name will be "config-bk-prompt-DATE.sh"